# 首版验证记录

验证日期：2026-09-06。此包为内部试用首版，不包含任何成员的真实 Token 或项目导出内容。

## 已验证

- Python 3.9 标准库运行；14 项自动测试通过，覆盖 Token Exchange/Bearer、401 单次刷新与脱敏、429 重试、重定向拒绝、项目和接口范围、分页和重复保护、日期时区边界、取消筛选、拆分保留、XLIFF 占位符和命名空间状态、Excel/CSV 公式安全、预览有效期/更改/单次使用、数量不符停止、Job 模式、安装更新保留个人凭证。
- 使用用户授权的测试 Token，EU Token Exchange 成功；四个白名单项目均可读取且名称与 UID 一致。Token 通过隐藏输入进入测试进程，未写入配置或发行包。
- 完整实测范围：Aniimo_TEP_CN_Multi_游戏内_Final，en，QC，dateCreated 从 2026-08-01T00:00:00+08:00 起、无结束上界、含已取消，分段 completed 口径。
- 实测 45 个 Job、38 个文件名、14,982 个分段、3,167 个 completed。单一 XLIFF 成功，独立进度接口核对一致。此处是验证时快照，不保证后来导出仍是相同数量。
- XLSX ZIP 完整性通过；另用 openpyxl 读取，逐行与独立解析结果比较全部原文和布尔完成值，核对文件 SHA-256、冻结首行、筛选和无公式单元格。
- Codex 插件 manifest 与 Skill 官方本地校验器通过；macOS 安装入口语法检查及隔离目录安装测试通过。发布前检查 ZIP 白名单内容，无实际 credentials.json、无导出数据、无疑似 Platform Token。

## 尚未实测 / 不承诺

- 未在 Windows 真机上执行 CMD 入口或验证企业 ACL/终端策略；请先安排一名 Windows 成员试装。
- 未将此包注册到当前用户的 Codex 插件面板；默认独立 Skill 安装已测试，原生插件注册路径仍需目标机器的 Codex CLI 验证。
- 未验证其他 AI 工具自动发现目录、沙箱/网络权限；通用脚本可移植不代表所有宿主的安装步骤相同。
- 未实测 US 租户、1000 Job/150 MB 极限、所有内联标签变体及网络代理。遇到未知格式程序停止，不静默丢数据。
- 本地配置和确认流程不是强制权限系统；明文 Token 的安全取决于用户电脑和 Phrase 权限。

维护人员可在解压目录运行 `python3 -m unittest discover -s tests -v` 复测离线测试。Windows 将 python3 换为 `py -3`。测试不需要真实 Token。
