# Phrase Export 1.0.0

这是一个可分享的本地工具包：Skill + 固定 Python 程序 + Codex 插件描述。没有服务器、没有 MCP 服务、没有第三方 Python 依赖。需要成员的 AI 工具能调用本地命令，并安装 Python 3.9+。

## 成员三步使用

1. 从可信内部渠道下载 ZIP 并完整解压。Mac 双击 `安装-Mac.command`，Windows 双击 `安装-Windows.cmd`。若 Mac 不允许执行，在终端进入解压目录运行 `python3 install.py`；Windows 也可运行 `py -3 install.py`。安装器不会自动下载安装 Python，不需要管理员权限。
2. 安装输出会显示 `credentials_file`。用本地文本编辑器打开该文件，保留 JSON 结构，把占位文字替换成自己的 Phrase **Platform API Token**。不要编辑 SKILL.md，也不要把凭证发给 AI。
3. 在支持本地 Skill 的 AI 工具中新建对话，例如：“使用 phrase-export，预览 Aniimo_TEP_CN_Multi_游戏内_Final 中北京时间 2026 年 8 月 1 日起创建、英语、QC 阶段的需求，包含已取消；导出单一 XLIFF 和三列 Excel，completed 按分段口径。”看到范围和数量后再确认导出。

默认安装为独立 Skill，位置是用户目录下 `.agents/skills/phrase-export`，不是 Codex 插件面板注册。Codex 可发现此用户级 Skill；其他 AI 工具如果不识别这个目录，需要按该工具的 Skill 规则导入完整 `phrase-export` 文件夹。不同工具的自动发现方式不保证一致，不能只复制 SKILL.md。

## 凭证在哪里

| 系统 | 默认个人凭证位置 |
| --- | --- |
| macOS / Linux | `~/.config/phrase-export/credentials.json` |
| Windows | `%APPDATA%\PhraseExport\credentials.json` |

文件格式：

```json
{"region": "eu", "platform_token": "自己的 Phrase Platform API Token"}
```

团队当前流程使用 EU。其他账号只有确认租户属于 US 时才改为 `us`。不支持把 TMS 登录 Token、JWT 或其他产品 Token 自动混着尝试。程序请求 Token Exchange，取得 JWT 后调用 TMS，JWT 只保存在进程内存中；运行结束不会缓存到磁盘。

明文文件不是加密存储：同一系统用户、管理员、备份软件、具有本地文件访问权的 AI 或恶意程序可能读到它。macOS/Linux 新建凭证文件权限为 600，Windows 依赖用户配置目录的 ACL，安装器没有额外强化 Windows ACL。不要放在共享/同步文件夹，不要提交 Git；`.gitignore` 也不是安全隔离。每位成员使用自己的低权限 Token。撤销或更换 Token 到 Phrase 操作；之后本地替换即可。

## 需要出现在 Codex 插件面板时

先把完整解压目录放到长期保留的位置，再在该目录运行：

```text
python3 install.py --mode codex
```

Windows 将 `python3` 替换为 `py -3`。此模式需要支持 `codex plugin` 的 CLI，会先注册本地 marketplace，再安装 `phrase-export`。不会把 ZIP 当成可直接双击导入的专有插件文件。请不要移动或删除注册的目录。注册后新建对话。

只选一种安装模式，避免同一 Skill 重复出现。若已有同名 `personal` marketplace，安装器停止且不覆盖，改用默认 Skill 模式或由维护人员配置独立团队 marketplace。安装器默认不修改已有 Codex 配置；只有显式选择 `--mode codex` 才调用插件注册命令。

## 输出与限制

- 分段模式：`merged.xliff`（单一完整 XLIFF）、`export.xlsx`、`export.csv`、`manifest.json`。Excel 为 A 文件名、B 原文、C 是否 completed（布尔值）。清单模式输出 Job 信息，不生成 XLIFF。
- 只选择一个工作流阶段，保留同名拆分 Job，不按文件名去重。起始日期包含，结束日期不包含。时间必须带时区。
- 分段 completed：XLIFF 的 final 或 locked，并与进度接口的 completedSegmentsCount 核对。它不等于整 Job 已交付。未知格式/标签或数量不符停止，不交付貌似完整的结果。
- CSV 为防公式执行，会给以公式触发字符开头的文字加单引号；需保留原文请以 XLIFF 和 XLSX 为准。Excel 超长单元格会报错而非截断。
- 单次最多 1000 个 Job、每次响应最多 150 MB、Excel 单表最多 1,048,575 条数据；超限请拆分范围。XLIFF 首版只支持 Phrase XLIFF 2.0。
- 预览有效期 15 分钟且仅使用一次，导出前重新核对 Job 清单/状态；失败后重新预览。云端数据持续变化，不承诺事务级快照。
- 项目白名单是工具防误操作规则，不是强制权限隔离；用户可编辑本地代码。真正访问范围由 Phrase 账号权限控制。导出确认由 Skill 对话流程执行，不是无法绕过的安全授权弹窗。
- 不上传文件，不修改项目、Job 或译文。程序不记录 Token；预览与 manifest 会包含项目名、文件名及 Job 元数据，仍需按团队资料保管。

## 更新、分享和排错

维护人员只分享原始发布 ZIP，不重新打包个人安装目录。更新默认 Skill 可运行 `python3 install.py --replace`，旧 Skill 备份保留在个人配置目录的 `skill-backups` 中（不放在 AI 自动发现 Skill 的目录里），credentials.json 原样保留。回滚前检查备份内容；切勿盲目删凭证。

先用脚本 `config-path` 查看路径，再运行 `auth-check`；不要用 cat/read 命令把凭证发到聊天。Token Exchange 失败排查 Token 类型、过期/撤销和地区；403 是项目/操作权限；网络或证书错误不是 Token 无效。程序不会禁用 TLS。

脚本命令示例（SCRIPT 替换为已安装脚本路径）：

```text
python3 "SCRIPT" config-path
python3 "SCRIPT" auth-check
python3 "SCRIPT" projects
python3 "SCRIPT" preview --request "request.json"
python3 "SCRIPT" export --preview ID --confirm ID --output "exports"
```

自定义配置可在子命令前使用 `--config "个人凭证路径"`。不要在参数中放 Token。

认证参考：[Phrase Platform authentication](https://developers.phrase.com/en/api/platform/authentication)。导出参考：[Download bilingual file](https://developers.phrase.com/en/api/tms/v1/job/download-bilingual-file)。
